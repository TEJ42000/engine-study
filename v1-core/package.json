{
  "name": "engine-study-v1-core",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic v1 (+v1.1) core: data model + verified pure logic. UI-agnostic; Cosmos builds the React app on top of this.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json && node dist/__checks__.js"
  }
}
